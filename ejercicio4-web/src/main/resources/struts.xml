<!DOCTYPE struts PUBLIC
"-//Apache Software Foundation//DTD Struts Configuration 2.0//EN"
"http://struts.apache.org/dtds/struts-2.0.dtd">

<struts>
    <constant name="struts.enable.DynamicMethodInvocation" value="false"/>
    <constant name="struts.devMode" value="true"/>
    <!-- Use Guice as the ObjectFactory for this application -->
    <constant name="struts.objectFactory" value="guice" />
    <constant name="struts.custom.i18n.resources" value="global" />
    <constant name="struts.action.excludePattern" value="j_security_check" />
    
    <package name="app-default" extends="struts-default, tiles-default">
        <interceptors>
            <interceptor-stack name="secure">
                <interceptor-ref name="roles">
                    <param name="allowedRoles">ADMIN</param>
                </interceptor-ref>
                <interceptor-ref name="paramsPrepareParamsStack"/>
            </interceptor-stack>
        </interceptors>
    </package>
    <!-- Books package -->
    <package name="books" extends="app-default">
        <!--<default-interceptor-ref name="secure"/>-->
        <!-- create book -->
        <action name="create" class="actions.books.BookAction">
            <result type="tiles">app.books.persist</result>
        </action>
        <action name="createProcess" class="actions.books.BookAction" method="persist">
            <result name="input" type="redirectAction">create</result>
            <result type="redirectAction">create</result>
        </action>
        <!-- update book -->
        <action name="update" class="actions.books.BookAction">
            <result type="tiles">app.books.persist</result>
        </action>
        <action name="updateProcess" class="actions.books.BookAction" method="persist">
            <result name="input" type="redirectAction">update</result>
            <result type="redirectAction">update</result>
        </action>
        <!-- Books list -->
        <action name="all" class="actions.books.BooksAction">
            <result type="tiles">app.books.all</result>
        </action>
    </package>
    <!-- Accounts package -->
    <package name="accounts" extends="app-default">
        <action name="login" class="actions.accounts.SigninAction">
            <interceptor-ref name="store">
                <param name="operationMode">RETRIEVE</param>
            </interceptor-ref>
            <result>/WEB-INF/views/accounts/login.jsp</result>
        </action>
        <action name="check" class="actions.accounts.SigninAction" method="check">
            <interceptor-ref name="store">
                <param name="operationMode">STORE</param>
            </interceptor-ref>
            <result name="input" type="redirectAction">login</result>
            <result type="redirectAction">login</result>
        </action>
        <action name="signup" class="actions.accounts.SignupAction">
            <interceptor-ref name="store">
                <param name="operationMode">RETRIEVE</param>
            </interceptor-ref>
            <result type="tiles">app.accounts.signup</result>
            <result name="input" type="tiles">app.accounts.signup</result>
        </action>
        <action name="create" class="actions.accounts.CreateUserAction">
            <interceptor-ref name="store">
                <param name="operationMode">STORE</param>
            </interceptor-ref>
            <interceptor-ref name="defaultStack" />
            <result name="input" type="redirectAction">signup</result>
            <result type="redirectAction">signup</result>
        </action>
    </package>
</struts>
